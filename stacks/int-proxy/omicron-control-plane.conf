upstream kube_apiservers {
  least_conn;

  server 10.73.95.94:6443 weight=1 max_fails=3 fail_timeout=10s;
}

server {
  listen 6443;
  proxy_pass kube_apiservers;
  proxy_timeout 10m;
  proxy_connect_timeout 1m;
}