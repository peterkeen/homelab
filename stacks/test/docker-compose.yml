services:
  testroot:
    image: s6on/alpine
    command: "s6-pause"

  testcron:
    image: alpine
    environment:
      - HOSTNAME
    command: ["echo", "$HOSTNAME"]
    profiles:
      - cron
    x-cron:
      schedule: "* * * * *"
